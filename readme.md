### Installation
```
pip install click-migr
```
### How To Use
```
usage: script.py [-h] --source-table SOURCE_TABLE [--source-database SOURCE_DATABASE] [--source-host SOURCE_HOST]
                 [--source-port SOURCE_PORT] [--source-address SOURCE_ADDRESS] [--source-username SOURCE_USERNAME]
                 [--source-password SOURCE_PASSWORD] --destination-table DESTINATION_TABLE
                 [--destination-database DESTINATION_DATABASE] [--destination-host DESTINATION_HOST]
                 [--destination-port DESTINATION_PORT] [--destination-address DESTINATION_ADDRESS]
                 [--destination-username DESTINATION_USERNAME] [--destination-password DESTINATION_PASSWORD]

options:
  -h, --help            show this help message and exit
  --source-table SOURCE_TABLE
  --source-database SOURCE_DATABASE
  --source-host SOURCE_HOST
  --source-port SOURCE_PORT
  --source-address SOURCE_ADDRESS
  --source-username SOURCE_USERNAME
  --source-password SOURCE_PASSWORD
  --destination-table DESTINATION_TABLE
  --destination-database DESTINATION_DATABASE
  --destination-host DESTINATION_HOST
  --destination-port DESTINATION_PORT
  --destination-address DESTINATION_ADDRESS
  --destination-username DESTINATION_USERNAME
  --destination-password DESTINATION_PASSWORD
```